{
  "name": "ab-tech",
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.0.22",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "run-htmx": "turbo run dev --filter=@repo/htmx_server"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
